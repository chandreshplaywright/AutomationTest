{
  "name": "playwright-test",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "test": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.16.2",
    "vite": "^7.0.2"
  }
}

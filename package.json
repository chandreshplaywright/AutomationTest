{
  "name": "playwright-test",
  "version": "0.0.0",
  "scripts": {
    "test": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.16.2",
    "axe-playwright": "^2.1.0"
  }
}
